SET TALK OFF
SET ECHO OFF
USE IPNHISTO
DO WHILE .NOT. EOF()
   IF CEDULA = "8.329.209"
      REPLACE CEDULA WITH "8.239.209"
   ENDIF
   SKIP
ENDDO
USE IPNHISTO INDEX IPNHISTO, IPNHIST2
REINDEX
CLOSE ALL
RETURN
